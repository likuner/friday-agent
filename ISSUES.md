# Friday Agent 问题盘点与演进方案

> 本文基于对当前代码的实际核查（含一次实测复现），盘点项目现存问题，并给出
> **Agent 记忆、多模态、CI/CD、云环境上线** 四条演进路线的落地方案。
>
> 核查时间：代码版本 `98c2836`。文中 `文件:行号` 引用可能随代码演进漂移，以描述为准。

---

## 目录

- [零、结论摘要](#零结论摘要)
- [一、最紧急的问题（P0）](#一最紧急的问题p0)
- [二、Agent 记忆方案](#二agent-记忆方案)
- [三、多模态能力演进](#三多模态能力演进)
- [四、CI/CD 流程](#四cicd-流程)
- [五、云环境上线](#五云环境上线)
- [六、其他问题清单](#六其他问题清单)
- [七、优先级路线图](#七优先级路线图)

---

## 零、结论摘要

| 级别 | 问题 | 影响 |
| --- | --- | --- |
| **P0** | Agent 是进程级单例，记忆跨会话/跨用户串味 | 隐私泄漏、答案被污染，**已实测复现** |
| **P0** | 传入的 `history` 被忽略，上下文实际来自全局单例 | 无法按会话隔离，重启即失忆 |
| **P0** | 无任何测试、无 Dockerfile、无 CI、无数据库迁移 | 无法安全迭代与上线 |
| **P1** | 验证码存在进程内存 dict | 多副本部署直接失效 |
| **P1** | `JWT_SECRET` 默认 `change-me`、`/files` 无鉴权、登录无限流 | 安全风险 |
| **P1** | 上传文件存本地磁盘 | 多副本不一致，需对象存储 |
| **P1** | ~~无 token 用量统计与成本可见性~~ → **已部分解决**：接入 OpenTelemetry 追踪后可在 AgentScope Studio 查看每次调用的 token 用量（落库统计与配额仍未做） | 成本可控性提升 |
| **P2** | 多模态仅支持图片 | PDF/音频/视频待扩展 |
| **P2** | 前端无错误边界、无虚拟列表 | 长会话性能与健壮性 |

---

## 一、最紧急的问题（P0）

### 1.1 Agent 单例导致记忆串味（已实测复现）

**现象**

`backend/app/agent.py` 末尾：

```python
agent_service = AgentService()          # 进程级单例，import 时创建
```

`AgentService.__init__` 里创建一个 AgentScope `Agent` 实例（`self._agent`），而 AgentScope 的
`Agent` **自带对话状态**（内部 memory）。也就是说：**整个后端进程只有一份对话记忆，被所有用户、
所有会话共享。**

**实测证据**

新建两个互不相干的会话 A / B（同一用户）：

```
会话 A：请只回复「收到」。另外记住一个词：紫色河马
        → 收到。

会话 B（全新，历史为空）：我刚才让你记住的那个词是什么？
        → 紫色河马          ← B 读到了 A 的内容
```

由于实例是进程级而非请求级，**跨用户同样会串味**：用户甲在会话里说的话，会进入用户乙提问时的上下文。
这既是正确性缺陷，也是**隐私事故**。

**线上真实复现（开发期偶发观测）**

在一次联调中，两个会话并发进行：一个会话连续问了几个关于「杭州」的问题，另一个会话（标题
《高血压怎么治疗好呢》，此前从未提过杭州）只问了一句「你在哪里嗯」——结果回答的开头是
「**杭州：历史沿革与名胜**」。即 A 会话的内容直接决定了 B 会话的回答方向。

这类污染在并发下几乎必然发生，且用户无从察觉（没有报错、没有异常，只是答非所问）。

**根因**

1. Agent 实例的作用域错误（应为「每个会话一个」或「每个请求重建」）。
2. 依赖框架内部隐式状态，而不是显式管理上下文。

**修复方向（按推荐度排序）**

| 方案 | 做法 | 优点 | 缺点 |
| --- | --- | --- | --- |
| **A. 无状态重建（推荐）** | 每次请求从库里取该会话最近 N 轮消息，显式拼成 `messages` 交给模型 | 天然多副本可扩展、无串味、重启不失忆 | 每轮要读库、要自己控 token 预算 |
| B. 每会话一个 Agent | 用 `conversation_id` 做 key 维护 Agent 池，配 `state` 持久化 | 复用框架能力 | 需要池化/淘汰策略，多副本仍要共享 state 存储 |
| C. 每请求新建 Agent | 简单直接 | 实现最省事 | 每次都要把历史喂进去（等于方案 A 的简化版），且丢失框架级压缩能力 |

方案 A 的伪代码：

```python
# chat.py
history = await load_recent_messages(db, conversation.id, limit=20)   # 按 token 预算截断
messages = [{"role": m.role, "content": m.content} for m in history]
async for event in agent_service.stream(messages, ...):   # 不再依赖任何跨请求状态
    ...
```

> 顺带修掉 1.2。

### 1.2 传入的 `history` 被忽略

`backend/app/chat.py:53-54` 认真构造了 `history` 并传给 `agent_service.stream(history, ...)`，
但 `backend/app/agent.py:138` 只取了最后一条：

```python
user_content = messages[-1]["content"]      # 前面的全部丢弃
```

**后果**：`history` 是个"看起来在工作"的死参数；真实上下文来自单例 Agent 的内部记忆。
这也解释了为什么单用户演示时"记忆"看起来正常——一旦重启进程就全部失忆，而会话记录明明还在库里。

### 1.3 没有请求级隔离

除了 Agent，`AgentService` 里的 `_agent` / `_thinking_agent` 两个实例都是共享的。
在多用户并发下还存在**并发安全**疑问：AgentScope `Agent` 的 `reply_stream` 是否可重入未被验证，
一旦并发调用同一个实例，可能出现事件流交叉。修复 1.1 后此问题自然消失。

---

## 二、Agent 记忆方案

### 2.1 现状

| 维度 | 现状 |
| --- | --- |
| 短期记忆 | **隐式**：靠单例 Agent 的内部状态，跨会话共享，重启丢失 |
| 长期记忆 | **完全没有** |
| 落库内容 | 对话消息全文落库（`messages` 表），但从不回放给模型 |
| Token 预算 | 无任何控制，上下文只增不减 |
| 压缩 | 未启用（AgentScope 提供 `context_config` 压缩能力，未使用） |
| 用户画像 | 无 |

### 2.2 短期记忆（会话内上下文）

**目标**：同一会话的多轮对话连贯，且不越界、不超预算。

1. **显式回放**：每轮从 `messages` 表读最近 N 轮，构造成模型消息。
2. **Token 预算**：以 token 数（而非条数）为准，预留 system prompt + 工具定义 + 本轮附件的额度；
   超出时从最旧的消息开始丢弃。
3. **滚动摘要**：被丢弃的历史用一次便宜模型的调用压缩成"会话摘要"，作为一条 system 消息常驻。
   建议落库到 `conversations.summary` + `summary_upto_message_id`，避免每轮重算。
4. **窗口策略**：最近 K 轮全文 + 更早内容的摘要，是当前性价比最高的组合。

```
┌───────────── 模型上下文预算 ─────────────┐
│ system prompt + 工具定义      （固定）    │
│ 会话摘要（滚动压缩）          （~500 tok）│
│ 最近 K 轮完整消息             （弹性）    │
│ 本轮用户消息 + 附件           （固定）    │
└──────────────────────────────────────────┘
```

### 2.3 长期记忆（跨会话）

**目标**：记住"这个用户是谁、长期偏好、既往结论"，而不是记住全部历史。

建议分三层，按需启用：

| 层 | 存储 | 写入时机 | 读取方式 |
| --- | --- | --- | --- |
| 结构化画像 | `user_profiles` 表（键值/JSONB） | 用户显式告知（"我是大三学生"）、或抽取器定期挖掘 | 每轮注入 system |
| 事实条目 | `user_memories` 表（内容 + 向量 + 置信度 + 来源消息 id） | 对话结束后异步抽取 | 按当前问题向量检索 Top-K 注入 |
| 历史检索 | 复用现有 `chunks` 的思路，对历史消息做 embedding | 消息入库后异步 | 用户问"上次那个方案"时召回 |

**必须配套的机制**（长期记忆最容易出事的地方）：

- **写入需要把关**：只存稳定事实与偏好，不存一次性上下文；低置信度条目先落"候选"。
- **时效与冲突**：条目带 `created_at` / `expires_at` / `superseded_by`，新事实覆盖旧事实而不是并存。
- **用户可见可控**：提供"我的记忆"页面，支持查看与删除（**遗忘权**，也是合规要求）。
- **注入要标注**：注入的记忆需在提示词里标明"这是历史记忆，可能过时"，避免模型当成当前事实。
- **成本与延迟**：检索+注入会加长 prompt，需要设上限并做缓存。

### 2.4 落地路线

1. **第一步（随 P0 一起做）**：显式回放 + token 预算 —— 先把"正确性"和"隔离性"拿到。
2. **第二步**：滚动摘要（长会话体验），落 `conversations.summary`。
3. **第三步**：结构化画像（投入产出比最高，实现简单）。
4. **第四步**：向量化事实条目 + 检索注入（需要评估效果再上，避免"记忆污染"）。

---

## 三、多模态能力演进

### 3.1 现状

| 项 | 现状 |
| --- | --- |
| 支持类型 | **仅图片**（png / jpg / webp / gif / bmp） |
| 校验方式 | Pillow 按真实内容嗅探格式，不信后缀与 Content-Type |
| 数量 / 单张体积 | 最多 9 张 / 单张 5MB |
| 传输方式 | 读文件 → base64 → `DataBlock(Base64Source)` → `OpenAIChatFormatter` → `image_url` |
| 存储 | 本地磁盘 `backend/files/`，静态目录 `/files/<uuid>.<ext>` |
| 引用方式 | 消息 `meta.attachments` 仅存文件名 |
| 前端 | 多选上传、缩略图、点击预览（antd Image） |

模型侧已验证：`deepseek-chat` 与 `deepseek-v4-flash` 均能识图（模型卡未声明但实测支持）。

### 3.2 扩展到各类媒体

不同模态的"送模型方式"完全不同，不能都塞进 `DataBlock`：

| 类型 | 推荐路径 | 关键工作 |
| --- | --- | --- |
| **PDF（文本型）** | 服务端抽取文本 → 分块 → 按相关性注入（复用 RAG 链路） | `pypdf`/`pdfplumber` 抽取；保留页码用于引用 |
| **PDF（扫描/图片型）** | 页面转图 → 视觉模型或 OCR | 渲染分辨率、页数上限、OCR 语言 |
| **音频** | ASR 转写 → 文本（主流做法）；或原生音频模型 | 语音转写（Whisper / 智谱 ASR）、说话人分离、时间戳、长音频分段 |
| **视频** | 抽帧（关键帧）+ 音轨转写，两路合并 | 抽帧策略、时长/帧数上限、音视频对齐、成本极高需强约束 |
| **表格 / 公式** | 专用解析（Markdown / LaTeX）而非纯文本抽取 | 结构还原，否则信息丢失严重 |

**通用工程要点**

1. **统一抽象**：新增 `attachments` 表，字段建议
   `id / user_id / kind(image|pdf|audio|video) / mime / size / storage_key / status(pending|parsing|ready|failed) / extracted_text / page_count / duration / error`。
   消息只引用 `attachment_id`，不再只存文件名。
2. **异步解析**：上传后立刻返回、后台解析（队列 / 后台任务），前端轮询或 SSE 获取进度。
   视频/长音频绝不能同步阻塞请求。
3. **状态机与失败可恢复**：`pending → parsing → ready/failed`，失败要能重试与提示。
4. **额度与成本**：按类型分别限制（PDF 页数、音频分钟数、视频时长），并在 UI 上明示。
   **解析结果先落文本，送模型时再按需裁剪**——不要每次都把原始媒体塞进上下文。
5. **安全**：媒体文件同样有恶意内容风险（PDF 脚本、超大解压比），需要沙箱解析与体积/页数硬上限。
6. **存储**：本地磁盘在多副本下必然不一致，应迁到对象存储（S3/OSS/MinIO）+ 签名 URL。
7. **前端**：不同媒体类型的渲染（PDF 分页预览、音频播放器、视频播放器）与上传进度。

### 3.3 建议顺序

**PDF 文本 → 音频转写 → PDF 扫描件 → 视频**。
视频放最后：收益相对低、成本与复杂度最高。

---

## 四、CI/CD 流程

### 4.1 现状：完全空白

核查结果：

```
.github/workflows    不存在
Dockerfile           0 个（compose 只起了 postgres）
测试文件             0 个（后端无 pytest，前端无 vitest/jest）
Alembic / migrations 不存在（启动时 create_all 建表）
lint 配置            前端 next lint 未配置规则；后端无 ruff/flake8
```

当前唯一的"质量保障"是手工执行 `python -m compileall` 与 `npx tsc --noEmit`。

### 4.2 建议流水线

**阶段一：PR 门禁（先做这个，成本最低收益最高）**

```yaml
# .github/workflows/ci.yml 概要
backend:  ruff check + pytest（含单元测试与接口测试，用 testcontainers 起 PG）
frontend: npm ci + tsc --noEmit + eslint + vitest + next build
compose:  docker compose config 校验
```

要跑起来需要先补三件事：**测试**、**lint 配置**、**可离线运行的依赖**（把 `agentscope` 等重型依赖的
单测打桩，避免 CI 里调真实模型）。

**阶段二：构建与镜像**

- 后端 `Dockerfile`：多阶段构建，`python:3.12-slim`，非 root 运行，`HEALTHCHECK`。
- 前端 `Dockerfile`：`next build` 后用 `output: 'standalone'` 产出最小运行镜像（需先在
  `next.config` 打开 standalone）。
- 打 tag 时构建镜像并推送到镜像仓库，镜像 tag 用 git sha，禁止 `latest` 上线。

**阶段三：部署与环境**

- 环境分离：`dev / staging / prod`，配置全部走环境变量或密钥管理，**不入库**。
- 数据库迁移作为部署的前置步骤（引入 Alembic 后，`alembic upgrade head` 独立执行，不与应用启动耦合）。
- 发布策略：滚动更新 + 就绪探针；有状态部分（Agent 记忆）修好后才谈多副本。
- 回滚：镜像 tag 可回退 + 迁移需保证向后兼容（先加列后删列，分两次发布）。

**阶段四：可观测与告警**

- 结构化日志（JSON）→ 采集；错误上报（Sentry 等）；关键指标（QPS、P95 延迟、模型失败率、token 消耗）。
- 上线门禁：错误率、P95 超阈值自动回滚。

---

## 五、云环境上线

### 5.1 当前阻塞项

| # | 阻塞项 | 说明 | 解决 |
| --- | --- | --- | --- |
| 1 | Agent 单例 | 多副本会串味，**必须先用 2.1 方案 A 改造** | 无状态化 |
| 2 | 验证码存进程内存 | `captcha.py` 用模块级 dict，多副本/重启即失效 | 迁 Redis |
| 3 | 上传文件在本地磁盘 | 副本间不一致，容器重建即丢 | 对象存储 + 签名 URL |
| 4 | `create_all` 建表 | 无法平滑演进表结构，多副本并发建表有竞态 | 引入 Alembic |
| 5 | `JWT_SECRET` 默认 `change-me` | 线上等于没有鉴权 | 密钥管理注入强随机值 |
| 6 | `/files` 静态目录无鉴权 | 任何人拿到 URL 即可访问 | 带鉴权的下载接口 / 签名 URL |
| 7 | CORS 写死 localhost | 线上跨域失败 | 环境变量配置 |
| 8 | 无反向代理 / HTTPS | 明文传输 JWT 与内容 | Nginx/ALB + 证书 |
| 9 | 登录/验证码无限流 | 暴力破解与验证码轰炸 | 限流（网关或 Redis 计数） |
| 10 | `/health` 不检查依赖 | 探针无法反映真实健康状况 | 增加 DB/Redis 连通性检查，区分 liveness/readiness |
| 11 | 无用量统计 | 模型成本不可见，无法做配额 | 记录 token/调用量 |

### 5.2 目标架构（单区域起步）

```
        ┌────────────┐
用户 ──▶ │  CDN / WAF │
        └─────┬──────┘
              ▼
        ┌───────────────────────────────┐
        │  负载均衡 / 反向代理（HTTPS）  │
        └───────┬───────────────┬───────┘
                ▼               ▼
        ┌──────────────┐  ┌──────────────┐
        │ 前端（静态）  │  │ 后端 API ×N  │   ← 无状态，可水平扩容
        └──────────────┘  └───┬──────┬───┘
                              │      │
              ┌───────────────┘      └──────────────┐
              ▼                                     ▼
     ┌─────────────────┐                 ┌──────────────────┐
     │ 托管 PostgreSQL │                 │ Redis            │
     │ （业务库）       │                 │ 验证码/限流/队列  │
     └─────────────────┘                 └──────────────────┘
              ┌─────────────────┐        ┌──────────────────┐
              │ pgvector（文献） │        │ 对象存储（附件）  │
              └─────────────────┘        └──────────────────┘
```

**要点**

- 后端**必须无状态**才能水平扩容（这是 5.1 第 1、2、3 条的目的）。
- 向量库可先用托管 PG + pgvector 扩展，规模上来再换专用向量数据库。
- 长任务（PDF/视频解析）需要独立 worker 与队列，不要占用 API 进程。
- 模型调用走统一网关（便于限流、重试、计费、多供应商切换）。

### 5.3 上线阶段

1. **单机容器化**：前后端 Dockerfile + compose 一键起（含 pg、pgvector、redis），
   验证容器化后功能与本地一致。
2. **托管化**：托管 PG + 对象存储 + Redis，后端跑在容器服务上（此时副本数仍为 1）。
3. **无状态化完成**：修完记忆与验证码，开多副本 + 自动扩缩。
4. **生产加固**：HTTPS、限流、密钥管理、监控告警、备份与演练、成本配额。

---

## 六、其他问题清单

### 6.1 安全

| 问题 | 说明 |
| --- | --- |
| `/files` 无鉴权 | 静态目录直接暴露，靠 UUID 不可枚举（安全性不足） |
| 登录/验证码无限流 | 无失败计数、无锁定、无 IP 限流，可暴力破解 |
| JWT 默认密钥 | `config.py` 默认 `change-me`，未在 `.env` 覆盖即等于无鉴权 |
| 无 Token 刷新/吊销 | 有效期 7 天，改密后旧 token 仍有效 |
| Token 存 localStorage | XSS 可直接窃取；可改为 httpOnly Cookie + CSRF 防护 |
| 上传校验之外无防护 | 无病毒扫描、无解压炸弹防护（图片尚可，扩到 PDF 后风险升高） |
| **提示词注入** | 用户输入与 RAG 检索到的文献内容都直接进模型上下文，无隔离与指令防护 |
| 日志可能含敏感信息 | 现在会记录用户消息前 100 字，线上需脱敏策略 |
| 无审计日志 | 谁在什么时候问过什么、管理员操作，均无记录 |

### 6.2 可靠性与性能

| 问题 | 位置/说明 |
| --- | --- |
| **人为流式限速** | `chat.py:83` 每个事件 `await asyncio.sleep(0.05)`，长回答被拖慢；应改为仅前端做打字机节奏 |
| 无断连处理 | 客户端断开后后端仍继续生成、继续计费；应检查 `request.is_disconnected()` 并取消 |
| 无超时与重试 | 模型 API 失败直接报错给用户，无退避重试与降级 |
| 无并发保护 | 单例 Agent 并发调用行为未验证（修 1.1 后消失） |
| 长会话无虚拟滚动 | 前端全量渲染 + Markdown 重排，几百条消息后会卡 |
| 会话列表无分页 | `list_conversations` 一次返回全部 |
| 无归档/清理 | 消息只增不减，无冷数据归档策略 |
| 附件无引用计数 | 删会话不删文件，孤儿文件只能手工跑 `cleanup_files.py` |

### 6.3 数据

| 问题 | 说明 |
| --- | --- |
| 无迁移工具 | `create_all` 无法表达改列/索引/数据回填 |
| `meta` JSONB 无 schema 版本 | `attachments` / `thinking` / `toolCalls` 结构变更后老数据无兼容依据 |
| 无用量与成本数据 | 没有 token 统计，成本不可观测、无法做配额 |
| 附件表缺位 | 图片只有文件名，没有元数据（大小/类型/解析状态），扩展多模态时必须补 |
| 无软删除 | 删除即物理删除，无回收站与误删保护 |

### 6.4 可观测性

**已解决（tracing）**：已接入 OpenTelemetry 追踪，`TracingMiddleware` 产出符合 GenAI 语义约定的
span，经 OTLP 导出到 AgentScope Studio，可查看 trace 树（Agent / 模型 / 工具调用）、token 用量、
耗时与完整属性。见根 README 的「可观测（AgentScope Studio）」章节。

**仍未做**：

- 无 metrics（QPS、P95 延迟、错误率、token 消耗趋势）—— 追踪是逐次明细，缺少聚合指标与告警。
- 无错误上报平台（Sentry 等），异常只能翻日志。
- 日志仅本地文件，容器化后随容器销毁而丢失，需集中采集。
- 缺少关键业务埋点（检索命中率、工具调用成功率、无答案率）。
- token 用量只在 trace 里，未落库，无法做配额与账单。

### 6.5 前端

| 问题 | 说明 |
| --- | --- |
| 无错误边界 | 缺 `error.tsx` / `loading.tsx` / `not-found.tsx`，组件抛错即白屏 |
| 无请求失败重试/提示 | 会话加载失败时静默 |
| 无虚拟列表 | 见 6.2 |
| 包体积 | antd 全量引入 + react-markdown，未做按需与代码分割 |
| 无 i18n | 文案硬编码中文 |
| 无移动端适配验证 | 折叠/抽屉逻辑有，但未系统测试 |
| 无前端测试 | 组件与交互零覆盖 |

### 6.6 工程规范

| 问题 | 说明 |
| --- | --- |
| 无测试 | 后端 0 测试、前端 0 测试 |
| 无 lint 规则 | ruff/eslint 均未配置规则集 |
| 前后端类型手工同步 | `lib/api.ts` 的类型手写，与后端 Pydantic 模型易漂移；可按 OpenAPI 生成 |
| 无 API 版本化 | 全部挂在 `/api` 下，破坏性变更无退路 |
| 演示模式与真实模式行为差异大 | `demo` 不走模型也不落真实流程，容易掩盖问题 |
| 无 CHANGELOG / 贡献指南 | 公开仓库缺 `CONTRIBUTING`、`LICENSE` |

### 6.7 产品与内容

- **"联网搜索"是占位功能**：目前只是往提示词里拼一句话，没有真正的检索能力；开关已隐藏但后端参数仍在。
- **"深度思考"依赖更换模型**：默认 `OPENAI_MODEL` 为 `deepseek-chat`，而该模型在 AgentScope 模型卡中已标记
  `status: sunset`；建议统一迁到 `deepseek-v4-flash` / `deepseek-v4-pro`。
- **文献语料不随仓库提供**：README 已说明，但新用户上手成本高，建议提供小规模示例语料与导入脚本。
- **医学免责声明**：提示词里已要求"急症建议就医"，但缺少产品层面的固定免责声明与合规提示。

---

## 七、优先级路线图

### 第一批：修正确性与安全（建议立刻做）

1. **Agent 记忆改造**：改为无状态重建（方案 A），同步修掉 `history` 被忽略的问题。
2. `JWT_SECRET` 强制校验（默认值直接拒绝启动）+ 登录/验证码限流。
3. `/files` 加鉴权或改签名 URL。
4. 补 `Dockerfile`（前后端）+ 引入 Alembic。

### 第二批：可迭代与可运维（1～2 个迭代）

5. 建立测试与 CI 门禁（PR 跑 lint + 类型检查 + 测试 + 构建）。
6. 验证码迁 Redis、附件迁对象存储 → 具备多副本能力。
7. 补可观测：结构化日志、错误上报、token 用量统计。
8. 去掉人为流式限速、加断连取消与超时重试。

### 第三批：能力扩展（按业务价值排序）

9. **PDF 文本抽取**（复用 RAG 链路，性价比最高）。
10. 短期记忆压缩（滚动摘要）+ 结构化用户画像。
11. 全局搜索：跨会话检索历史消息。
12. 音频转写 → 视频理解。

### 第四批：规模化

13. 无状态水平扩容 + 自动扩缩。
14. 长任务拆到独立 worker 与队列。
15. 模型网关化（限流、重试、多供应商、计费）。
16. 长期记忆的向量化事实条目（需先做效果评估与"记忆污染"防护）。

---

## 附：快速自检命令

```bash
# 后端语法检查（当前唯一可用的静态检查）
cd backend && .venv/bin/python -m compileall -q app

# 前端类型检查
cd frontend && npx tsc --noEmit

# 附件孤儿文件检查（只预览，不删除）
cd backend && .venv/bin/python scripts/cleanup_files.py

# 复现记忆串味问题（两个新会话）
#   会话 A：记住一个词：紫色河马
#   会话 B：我刚才让你记住的词是什么？  → 若答出即复现
```
