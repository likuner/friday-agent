# Friday Agent 重点问题与演进方案

> 本文基于对当前代码的核查与实测，盘点项目的**重点问题**，并给出
> **Agent 记忆、多模态、CI/CD、云环境上线** 四条演进路线。
>
> 代码版本：`997e922`。文中 `文件:行号` 会随代码演进漂移，以描述为准。
> 上一版（`85c3b25`）之后的进展见 [附录 A](#附录-a相较上一版的变化)。

---

## 目录

- [零、重点问题总览](#零重点问题总览)
- [一、P0：必须先修](#一p0必须先修)
- [二、P1：上线前必须解决](#二p1上线前必须解决)
- [三、Agent 记忆方案](#三agent-记忆方案)
- [四、多模态能力演进](#四多模态能力演进)
- [五、CI/CD 与云环境上线](#五cicd-与云环境上线)
- [六、其他问题清单](#六其他问题清单)
- [七、优先级路线图](#七优先级路线图)
- [附录 A：相较上一版的变化](#附录-a相较上一版的变化)
- [附录 B：快速自检](#附录-b快速自检)

---

## 零、重点问题总览

| 级别 | 问题 | 影响 | 状态 |
| --- | --- | --- | --- |
| **P0** | Agent 是进程级单例，记忆跨会话/跨用户串味 | 隐私泄漏、答非所问，且**无法水平扩容** | 未修，已两次实证 |
| **P0** | 传入的 `history` 被忽略，上下文来自单例内部状态 | 上下文不可控、重启即失忆（库里记录却还在） | 未修 |
| **P0** | 无测试 / 无 CI / 无 Dockerfile / 无数据库迁移 | 无法安全迭代，也无法上线 | 未修 |
| **P1** | `JWT_SECRET` 默认 `change-me`、登录无限流、`/files` 无鉴权 | 鉴权形同虚设、可暴力破解、图片可被任意读取 | 未修 |
| **P1** | 验证码存进程内存、上传文件存本地磁盘 | **多副本部署直接失效** | 未修 |
| **P1** | 无附件表，消息只存文件名 | 无类型/大小/解析状态，扩展 PDF/音视频时必须补 | 未修 |
| **P1** | 提示词注入无防护 | 用户输入与 RAG 文献内容直接进模型 | 未修 |
| **P2** | 多模态仅支持图片 | PDF / 音频 / 视频待扩展 | 部分（图片已支持） |
| **P2** | 前端无错误边界、无虚拟列表 | 组件抛错即白屏；长会话卡顿 | 未修 |
| **P2** | 无 metrics / 告警 / 错误上报 | 只有 trace 明细，缺聚合指标与告警 | 部分（tracing 已接入） |

**已经解决**（不再列为问题）：可观测追踪与 token 用量可见、图片失效降级、孤儿文件清理、深色主题闪烁、
流式中止、多图上传与预览。

---

## 一、P0：必须先修

### 1.1 Agent 单例导致记忆串味

**代码事实**

```python
# backend/app/agent.py:206
agent_service = AgentService()          # 进程级单例，import 时创建
```

`AgentService` 内部持有 AgentScope `Agent` 实例，而 `Agent` **自带对话状态**。
即：**整个后端进程只有一份对话记忆，被所有用户、所有会话共享。**

**两次实证**

*构造复现*——两个互不相干的会话：

```
会话 A：请只回复「收到」。另外记住一个词：紫色河马   → 收到。
会话 B（全新，历史为空）：我刚才让你记住的词是什么？  → 紫色河马
```

*真实发生*——联调期间两个会话并发，一个在问杭州，另一个（标题《高血压怎么治疗好呢》、
此前从未提过杭州）只问了一句「你在哪里嗯」，回答开头却是「**杭州：历史沿革与名胜**」。

这类污染在并发下几乎必然发生，而且**不报错、不异常，只是答非所问**，用户无从察觉。

**影响面**

1. 正确性：回答被无关会话的内容带偏。
2. 隐私：用户甲说的话会进入用户乙的上下文。
3. 架构：实例有状态 → **不可能多副本部署**（P1 的多副本目标因此被卡住）。
4. 并发副作用：观测到**一个会话中止生成时，另一个正在生成的会话也被中断**——共享同一个 Agent 实例。

**修复方案（推荐 A）**

| 方案 | 做法 | 评价 |
| --- | --- | --- |
| **A. 无状态重建** | 每轮从库里取该会话最近 N 条消息，显式拼成 `messages` 交给模型 | ✅ 天然多副本、无串味、重启不失忆；需要自己做 token 预算 |
| B. 每会话一个 Agent | 用 `conversation_id` 维护 Agent 池 + `state` 持久化 | 复用框架能力，但需池化/淘汰，多副本仍要共享 state 存储 |
| C. 每请求新建 Agent | 最简单 | 等价于 A 的简化版，且丢掉框架级压缩能力 |

```python
# chat.py 方向性示意
history = await load_recent_messages(db, conversation.id, limit=20)   # 按 token 预算截断
messages = [{"role": m.role, "content": m.content} for m in history]
async for event in agent_service.stream(messages, ...):   # 不再依赖任何跨请求状态
    ...
```

> 这一步同时修掉 1.2，并且是后面「多副本」「长会话摘要」的前置条件。

### 1.2 传入的 `history` 被忽略

`backend/app/chat.py` 认真构造了 `history` 并传下去，但 `backend/app/agent.py:141` 只取最后一条：

```python
user_content = messages[-1]["content"]      # 前面的全部丢弃
```

所以 `history` 是个**看起来在工作**的死参数；真实上下文来自单例 Agent 的内部记忆。
这也解释了为什么单用户演示时"记忆"看着正常——**一旦重启进程就全部失忆，而库里的对话记录明明还在**。

### 1.3 无测试 / 无 CI / 无镜像 / 无迁移

核查结果（当前仍为 0）：

```
.github/workflows    0
Dockerfile           0        (compose 只起了 postgres)
测试文件             0        (后端无 pytest，前端无 vitest/jest)
Alembic / migrations 0        (启动时 create_all 建表)
LICENSE              无
```

当前唯一的"质量保障"是手工跑 `python -m compileall` 与 `npx tsc --noEmit`。
**没有测试就无法安全重构 1.1**（那是牵动核心链路的改动），所以这四项是 1.1 的前置。

---

## 二、P1：上线前必须解决

| # | 问题 | 说明 | 解决方向 |
| --- | --- | --- | --- |
| 1 | `JWT_SECRET` 默认 `change-me` | 未在 `.env` 覆盖即等于无鉴权 | 启动时校验，弱值直接拒绝启动 |
| 2 | 登录 / 验证码无限流 | 可暴力破解、验证码轰炸 | Redis 计数 + IP/账号维度限流 + 失败锁定 |
| 3 | `/files` 静态目录无鉴权 | `app.mount("/files", ...)` 直接暴露，仅靠 UUID 不可枚举 | 改为带鉴权的下载接口或签名 URL（带过期） |
| 4 | 验证码存进程内存 | `captcha.py` 用模块级 dict | 迁 Redis（否则多副本下验证码校验随机失败） |
| 5 | 上传文件存本地磁盘 | 副本间不一致，容器重建即丢 | 对象存储（S3/OSS/MinIO）+ 签名 URL |
| 6 | 无附件表 | 只有 `users/conversations/messages` 三张表，附件仅以文件名存在 `meta.attachments` | 建 `attachments` 表：类型/大小/状态/解析结果 |
| 7 | 提示词注入 | 用户输入与 RAG 检索到的文献内容直接进模型上下文，无隔离与指令防护 | 内容分隔标记、指令与数据分离、输出侧校验 |
| 8 | Token 无刷新/吊销 | 有效期 7 天，改密后旧 token 仍有效 | 引入 refresh token 与吊销列表 |
| 9 | Token 存 localStorage | XSS 可直接窃取 | 改 httpOnly Cookie + CSRF 防护 |
| 10 | 无审计日志 | 无人知道谁在何时问了什么 | 关键操作与访问审计 |
| 11 | `create_all` 建表 | 无法演进表结构，多副本并发建表有竞态 | 引入 Alembic |

**另有若干已实现但需注意的行为**：

- **附件只存文件名**：`meta.attachments` 里是文件名，**没有引用计数**。删除会话不会删文件，
  孤儿文件只能手工跑 `scripts/cleanup_files.py`（该脚本按"是否被消息引用"判断，安全但需人工执行）。
  建议改为引用计数或定期任务。
- **文件丢失无感知**：文件被删后前端会降级为「图片已失效」占位，但**没有任何监控告警**，
  服务端也不会主动发现。「被引用但文件已丢失」目前只有跑清理脚本时才会打印出来。

---

## 三、Agent 记忆方案

### 3.1 现状

| 维度 | 现状 |
| --- | --- |
| 短期记忆 | **隐式**：靠单例 Agent 内部状态，跨会话共享，重启丢失 |
| 长期记忆 | **完全没有** |
| 落库内容 | 消息全文落库，但从不回放给模型 |
| Token 预算 | 无控制，上下文只增不减 |
| 压缩 | 未启用（AgentScope 提供 `context_config`，未使用） |
| 用户画像 | 无 |

### 3.2 短期记忆（会话内）

目标：同一会话连贯，不越界、不超预算。

1. **显式回放**：每轮读该会话最近 N 条消息构造模型消息。
2. **Token 预算**：以 token 数为准（不是条数），预留 system prompt + 工具定义 + 本轮附件额度。
3. **滚动摘要**：被丢弃的历史用便宜模型压缩成"会话摘要"常驻，落 `conversations.summary`
   + `summary_upto_message_id`，避免每轮重算。
4. **窗口策略**：最近 K 轮全文 + 更早内容摘要。

```
┌───────────── 模型上下文预算 ─────────────┐
│ system prompt + 工具定义      （固定）    │
│ 会话摘要（滚动压缩）          （~500 tok）│
│ 最近 K 轮完整消息             （弹性）    │
│ 本轮用户消息 + 附件           （固定）    │
└──────────────────────────────────────────┘
```

### 3.3 长期记忆（跨会话）

| 层 | 存储 | 写入时机 | 读取方式 |
| --- | --- | --- | --- |
| 结构化画像 | `user_profiles`（JSONB） | 用户显式告知 / 定期抽取 | 每轮注入 system |
| 事实条目 | `user_memories`（内容 + 向量 + 置信度 + 来源消息 id） | 对话结束异步抽取 | 按当前问题向量检索 Top-K |
| 历史检索 | 对历史消息做 embedding | 消息入库后异步 | "上次那个方案"式召回 |

**必须配套**（长期记忆最容易出事的地方）：

- **写入把关**：只存稳定事实与偏好，低置信度先落候选。
- **时效与冲突**：带 `created_at` / `expires_at` / `superseded_by`，新事实覆盖旧事实而非并存。
- **用户可见可控**：提供"我的记忆"页面支持查看与删除（**遗忘权**，也是合规要求）。
- **注入标注**：标明"这是历史记忆，可能过时"，避免模型当成当前事实。
- **成本控制**：检索+注入会加长 prompt，需设上限并缓存。

### 3.4 落地顺序

1. **随 P0 一起做**：显式回放 + token 预算（拿到正确性与隔离性）。
2. 滚动摘要（长会话体验）。
3. 结构化画像（投入产出比最高）。
4. 向量化事实条目（需先评估效果，防"记忆污染"）。

---

## 四、多模态能力演进

### 4.1 现状（已支持图片）

| 项 | 现状 |
| --- | --- |
| 支持类型 | 图片（png / jpg / webp / gif / bmp） |
| 校验 | Pillow 按**真实内容**嗅探格式，不信后缀与 Content-Type |
| 数量 / 体积 | 最多 9 张 / 单张 5MB |
| 送模型 | 读文件 → base64 → `DataBlock(Base64Source)` → `OpenAIChatFormatter` → `image_url` |
| 存储 | 本地磁盘 `backend/files/`，静态目录 `/files/<uuid>.<ext>` |
| 引用 | 消息 `meta.attachments` 只存文件名 |
| 前端 | 多选上传、缩略图、点击预览（antd Image） |

> 注：`deepseek-chat` 与 `deepseek-v4-flash` 均能识图（模型卡未声明但实测支持）。
> 另外，为支持图片把 formatter 从 `DeepSeekChatFormatter` 换成了 `OpenAIChatFormatter`
> （前者会跳过 `DataBlock`）；纯文本与工具调用路径实测同样正常。

### 4.2 扩展到各类媒体

不同模态的"送模型方式"完全不同，不能都塞进 `DataBlock`：

| 类型 | 推荐路径 | 关键工作 |
| --- | --- | --- |
| **PDF（文本型）** | 服务端抽文本 → 分块 → 按相关性注入（复用 RAG 链路） | `pypdf`/`pdfplumber`；保留页码用于引用 |
| **PDF（扫描件）** | 页面转图 → 视觉模型 / OCR | 渲染分辨率、页数上限、OCR 语言 |
| **音频** | ASR 转写 → 文本（主流做法）；或原生音频模型 | 转写、说话人分离、时间戳、长音频分段 |
| **视频** | 抽帧（关键帧）+ 音轨转写，两路合并 | 抽帧策略、时长/帧数上限、音视频对齐、成本极高 |
| **表格 / 公式** | 专用解析（Markdown / LaTeX）而非纯文本抽取 | 结构还原，否则信息丢失严重 |

**通用工程要点**

1. **统一抽象**：新增 `attachments` 表（见 P1-6），消息引用 `attachment_id` 而非文件名。
2. **异步解析**：上传后立即返回、后台解析（队列 / worker），前端轮询或 SSE 获取进度。
   视频/长音频绝不能同步阻塞请求。
3. **状态机与可恢复**：`pending → parsing → ready/failed`，失败可重试。
4. **额度与成本**：按类型分别限制（PDF 页数、音频分钟数、视频时长）并在 UI 明示；
   **解析结果先落文本，送模型时再按需裁剪**，不要每次塞原始媒体。
5. **安全**：媒体文件同样有风险（PDF 脚本、解压炸弹），需沙箱解析与硬上限。
6. **存储**：迁对象存储（P1-5）。
7. **前端**：分类型渲染（PDF 分页、音频播放器、视频播放器）与上传进度。

### 4.3 建议顺序

**PDF 文本 → 音频转写 → PDF 扫描件 → 视频**。视频放最后：收益相对低、成本与复杂度最高。

---

## 五、CI/CD 与云环境上线

### 5.1 CI/CD（现状：完全空白）

**阶段一：PR 门禁**（成本最低、收益最高）

```yaml
backend:  ruff check + pytest（testcontainers 起 PG，模型调用打桩）
frontend: npm ci + tsc --noEmit + eslint + vitest + next build
compose:  docker compose config 校验
```

前置：补测试、补 lint 配置、把 `agentscope` 等重型依赖在测试中打桩（避免 CI 调真实模型）。

**阶段二：构建与镜像**
- 后端：多阶段构建、`python:3.12-slim`、非 root、`HEALTHCHECK`。
- 前端：`next build` + `output: 'standalone'`（当前无 `next.config`，需先加）。
- tag 触发构建推送，镜像 tag 用 git sha，禁止 `latest` 上线。

**阶段三：部署**
- 环境分离 `dev / staging / prod`，配置走环境变量或密钥管理。
- 迁移独立执行（引入 Alembic 后 `alembic upgrade head`，不与应用启动耦合）。
- 滚动更新 + 就绪探针；**记忆无状态化完成后**才谈多副本。
- 回滚：镜像 tag 可回退 + 迁移向后兼容（先加列后删列，分两次发布）。

**阶段四：可观测与告警**
- 已有 trace（见 6.3），补：结构化日志采集、错误上报、聚合指标与告警。
- 上线门禁：错误率 / P95 超阈值自动回滚。

### 5.2 云环境阻塞项

| # | 阻塞项 | 解决 |
| --- | --- | --- |
| 1 | Agent 单例 | **必须先做 §1.1 无状态化** |
| 2 | 验证码存进程内存 | 迁 Redis |
| 3 | 上传文件在本地磁盘 | 对象存储 |
| 4 | `create_all` 建表 | 引入 Alembic |
| 5 | `JWT_SECRET` 默认值 | 密钥管理注入强随机值 |
| 6 | `/files` 无鉴权 | 带鉴权下载 / 签名 URL |
| 7 | CORS 写死 | 环境变量配置 |
| 8 | 无反向代理 / HTTPS | Nginx/ALB + 证书 |
| 9 | 登录无限流 | 网关或 Redis 限流 |
| 10 | `/health` 不检查依赖 | 区分 liveness / readiness，检查 DB/Redis |
| 11 | 无用量统计与配额 | 已有 trace 维度数据，需落库做配额与账单 |

### 5.3 目标架构

```
        ┌────────────┐
用户 ──▶ │  CDN / WAF │
        └─────┬──────┘
              ▼
        ┌───────────────────────────────┐
        │  负载均衡 / 反向代理（HTTPS）  │
        └───────┬───────────────┬───────┘
                ▼               ▼
        ┌──────────────┐  ┌──────────────┐
        │ 前端（静态）  │  │ 后端 API ×N  │   ← 无状态，可水平扩容
        └──────────────┘  └───┬──────┬───┘
                              │      │
              ┌───────────────┘      └──────────────┐
              ▼                                     ▼
     ┌─────────────────┐                 ┌──────────────────┐
     │ 托管 PostgreSQL │                 │ Redis            │
     │ （业务库）       │                 │ 验证码/限流/队列  │
     └─────────────────┘                 └──────────────────┘
              ┌─────────────────┐        ┌──────────────────┐
              │ pgvector（文献） │        │ 对象存储（附件）  │
              └─────────────────┘        └──────────────────┘
```

### 5.4 上线阶段

1. **单机容器化**：前后端 Dockerfile + compose 一键起，验证容器化后功能一致。
2. **托管化**：托管 PG + 对象存储 + Redis（副本数仍为 1）。
3. **无状态化完成**：修完记忆与验证码，开多副本 + 自动扩缩。
4. **生产加固**：HTTPS、限流、密钥管理、监控告警、备份演练、成本配额。

---

## 六、其他问题清单

### 6.1 可靠性与性能

| 问题 | 位置/说明 |
| --- | --- |
| **人为流式限速** | `chat.py` 每个事件 `await asyncio.sleep(0.05)`，长回答被拖慢；应只在前端做打字机节奏 |
| 断连成本 | 已能感知断连并保存部分内容，但**模型侧取消是否立即停止计费未验证** |
| 无超时与重试 | 模型 API 失败直接报错，无退避重试与降级 |
| 长会话无虚拟滚动 | 前端全量渲染 + Markdown 重排，几百条消息后卡顿 |
| 会话列表无分页 | `list_conversations` 一次返回全部 |
| 消息只增不减 | 无冷数据归档策略 |
| 附件无引用计数 | 删会话不删文件（见 P1 说明） |

### 6.2 数据

| 问题 | 说明 |
| --- | --- |
| 无迁移工具 | `create_all` 无法表达改列/索引/回填 |
| `meta` JSONB 无 schema 版本 | `attachments` / `thinking` / `toolCalls` / `stopped` 结构变更后老数据无兼容依据 |
| 无 token 用量落库 | trace 里有，但未进业务库，无法做配额与账单 |
| 附件表缺位 | 见 P1-6 |
| 无软删除 | 删除即物理删除，无回收站与误删保护 |

### 6.3 可观测性

- ✅ **已接入** OpenTelemetry 追踪（`TracingMiddleware` + OTLP → AgentScope Studio），
  可看 trace 树（Agent / 模型 / 工具）、token 用量、耗时与 GenAI 语义属性。
- ❌ 无 **metrics**（QPS、P95、错误率、token 趋势）—— 只有逐次明细，缺聚合与告警。
- ❌ 无错误上报平台，异常只能翻日志。
- ❌ 日志仅本地文件，容器化后随容器销毁而丢失，需集中采集。
- ❌ 缺业务埋点（检索命中率、工具调用成功率、无答案率）。

### 6.4 前端

| 问题 | 说明 |
| --- | --- |
| 无错误边界 | 缺 `error.tsx` / `loading.tsx` / `not-found.tsx`，组件抛错即白屏 |
| 无虚拟列表 | 见 6.1 |
| 包体积 | antd 全量引入 + react-markdown，未按需与代码分割 |
| 无 i18n | 文案硬编码中文 |
| 无前端测试 | 组件与交互零覆盖 |
| **antd 样式覆盖坑** | antd 的 CSS-in-JS 后注入，同优先级下会盖掉 Tailwind 的 `background-color` / `display` 等；已多次踩坑（图标 `hidden` 失效、按钮被撑成椭圆）。新增交互元素时优先用原生 `<button>` + Tailwind，或内联 style |

### 6.5 工程规范

| 问题 | 说明 |
| --- | --- |
| 无测试 | 后端 0、前端 0 |
| 无 lint 规则 | ruff / eslint 均未配置规则集 |
| 前后端类型手工同步 | `lib/api.ts` 类型手写，与后端 Pydantic 易漂移；可按 OpenAPI 生成 |
| 无 API 版本化 | 全挂 `/api`，破坏性变更无退路 |
| 演示模式差异大 | `demo` 不走真实流程，容易掩盖问题 |
| 缺 `LICENSE` / `CONTRIBUTING` | 公开仓库缺这两项 |

### 6.6 产品与内容

- **"联网搜索"是占位功能**：只是往提示词里拼一句话，无真实检索能力（开关已隐藏，后端参数仍在）。
- **默认模型已 sunset**：`deepseek-chat` / `deepseek-reasoner` 在 AgentScope 模型卡中标记
  `status: sunset`，建议统一迁到 `deepseek-v4-flash` / `deepseek-v4-pro`。
- **文献语料不随仓库提供**：新用户上手成本高，建议提供小规模示例语料 + 导入脚本。
- **医学免责声明**：提示词里已要求"急症建议就医"，但缺产品层面的固定免责声明与合规提示。

---

## 七、优先级路线图

### 第一批：修正确性与安全（立刻做）

1. **Agent 记忆改造**（§1.1 方案 A）—— 同时修掉 `history` 死参数，并解锁多副本。
2. `JWT_SECRET` 启动校验 + 登录/验证码限流。
3. `/files` 加鉴权或改签名 URL。
4. 补测试（先覆盖鉴权、会话、SSE 三条主链路）→ 才能安全做第 1 项。

### 第二批：可迭代与可运维（1～2 个迭代）

5. CI 门禁（lint + 类型检查 + 测试 + 构建）。
6. Dockerfile（前后端）+ 引入 Alembic。
7. 验证码迁 Redis、附件迁对象存储 → 具备多副本能力。
8. 补 metrics / 错误上报 / 集中日志；token 用量落库。

### 第三批：能力扩展（按业务价值）

9. **PDF 文本抽取**（复用 RAG 链路，性价比最高）。
10. 短期记忆压缩 + 结构化用户画像。
11. 全局搜索：跨会话检索历史消息。
12. 音频转写 → 视频理解。

### 第四批：规模化

13. 无状态水平扩容 + 自动扩缩。
14. 长任务拆独立 worker 与队列。
15. 模型网关化（限流、重试、多供应商、计费）。
16. 长期记忆向量化事实条目（需先做效果评估与"记忆污染"防护）。

---

## 附录 A：相较上一版的变化

**新增能力**（已上线，不再是问题）

- 多模态图片：多选上传（≤9 张）、base64 送模型、点击预览、失效降级
- 中止生成：停止按钮 + 后端落库部分内容
- 深色主题：cookie 服务端渲染，无闪烁
- **可观测**：OpenTelemetry 追踪 → AgentScope Studio（trace 树、token 用量、耗时）

**新增认知**

- P0 记忆串味**从构造复现升级为真实发生**（用户会话里出现另一会话的内容）
- 单例还带来并发副作用：**一个会话中止会中断另一个会话的生成**
- antd CSS-in-JS 覆盖 Tailwind 的坑（`background-color`、`display`）反复出现，已记为 6.4 的注意事项

**结论未变**：P0 仍是同一个——Agent 状态的作用域错误。它同时卡住"正确性、隐私、多副本"三件事，
应当作为下一步的第一优先级。

---

## 附录 B：快速自检

```bash
# 后端语法检查（当前唯一可用的静态检查）
cd backend && .venv/bin/python -m compileall -q app

# 前端类型检查
cd frontend && npx tsc --noEmit

# 附件孤儿文件检查（只预览，不删除；会同时报告"被引用但已丢失"）
cd backend && .venv/bin/python scripts/cleanup_files.py

# 复现记忆串味：新建两个会话
#   会话 A：记住一个词：紫色河马
#   会话 B：我刚才让你记住的词是什么？   → 若答出即复现

# 查看运行追踪（需先启动 Studio 与开启 TRACING_ENABLED）
npm install -g @agentscope/studio && PORT=3001 as_studio
#   打开 http://localhost:3001 → Traces
```
